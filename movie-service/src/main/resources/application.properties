package com.example.movieticket;  // Ensure this matches the package of your other classes

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // Marks this as the Spring Boot application class
public class MovieTicketServiceApplication {

    public static void main(String[] args) {
        // This is the entry point to start the Spring Boot application
        SpringApplication.run(MovieTicketServiceApplication.class, args);
    }
}

